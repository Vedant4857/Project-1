#include <iostream>

int main() {
    int a, b;
    
    // Input two integers
    std::cout << "Enter two numbers: ";
    std::cin >> a >> b;
    
    // Calculate and output the sum
    int sum = a + b;
    std::cout << "Sum: " << sum << std::endl;

    return 0;
}
